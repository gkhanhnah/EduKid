/**
 * Behavior API — delegates to shared client (real Axios + mock mode in api.js).
 */
import {
  getBehaviors,
  getBehaviorStats,
  createBehavior,
} from './api.js'

export { getBehaviors, getBehaviorStats, createBehavior }
